# Onboarding scope / bloat watch

First-visit is **How-to only** (Device tab hidden in DOM). The How-to cycle has grown — track for a later trim.

## Current How-to beats (approx.)

0. Idle — controls hint  
1. Volume → Estudo  
2. Coluna 1 find section  
3. TOC click → coluna 2  
4. Select-to-search (termo)  
5. Provider click → Consulte  
6. Results in same column (termo article)  
7. Link click → ipsum article (+ focus)  
8. Select-to-annotate  
9. Notes coluna 4  
10. Bom proveito  

Plus: PT caption **TTS** (mute default; user unmutes) holds each beat when unmuted; silent timed holds when muted. **No auto-start** — idle until play. Cursor motion still has its own waits.

## Why it may bloat

- Two consult paths (select-to-search **and** link)  
- Dual articles (termo / ipsum)  
- TTS duration stacks with cursor animation (already ×1.25)  
- Captions reference all four columns explicitly  

## Trim ideas (when needed)

- Drop link path **or** select-to-search (keep one consult demo)  
- Merge “results below” into provider caption (one utterance)  
- Skip focus-only consult beat if article swap is obvious  
- Offer “skip narration” / faster rate for returning users  
- Re-enable Device only as optional, never default  

## Do not

- Add more beats without removing one  
- Duplicate empty-state Consulte copy into captions unless the cursor shows it  
